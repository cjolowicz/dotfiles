# -*-sh-*-

if [ -f ~/.ssh/agent.sh ] ; then
    . ~/.ssh/agent.sh
fi

if ! ps -p "$SSH_AGENT_PID" >/dev/null 2>&1 ; then
    rm -f "$SSH_AUTH_SOCK"
    ssh-agent | head -n 2 >~/.ssh/agent.sh
    . ~/.ssh/agent.sh
fi
